
/**
 *  save the user full name 
 *  @type {string} 
 */
const userName = "Isaac Grimaldo"


/** 
 * control the user loop 
 * @type {boolean} 
 */
const control = "dsadas"


/**
 *  scores list
 * @type {Array<Number>}
 */
const data = [10 , 50 , 90, 60, 80]

/**
 * items list
 * @type {Array<Number | String>} 
 */

const items = ["swords", 10, "food", 80]

